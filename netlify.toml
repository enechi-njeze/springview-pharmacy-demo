[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Friendly path: /api/concierge -> the function
[[redirects]]
  from = "/api/concierge"
  to = "/.netlify/functions/concierge"
  status = 200
